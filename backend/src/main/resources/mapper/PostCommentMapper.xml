<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.learnclaudedemo.mapper.PostCommentMapper">
    <resultMap id="BaseResultMap" type="com.example.learnclaudedemo.entity.PostComment">
        <id column="id" property="id"/>
        <result column="post_id" property="postId"/>
        <result column="user_id" property="userId"/>
        <result column="parent_id" property="parentId"/>
        <result column="reply_to_user_id" property="replyToUserId"/>
        <result column="content" property="content"/>
        <result column="like_count" property="likeCount"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, post_id, user_id, parent_id, reply_to_user_id, content, like_count, status, create_time
    </sql>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM post_comment WHERE id = #{id}
    </select>

    <select id="selectByPostId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM post_comment WHERE post_id = #{postId} ORDER BY create_time ASC
    </select>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM post_comment WHERE user_id = #{userId}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO post_comment (post_id, user_id, parent_id, reply_to_user_id, content, like_count, status, create_time)
        VALUES (#{postId}, #{userId}, #{parentId}, #{replyToUserId}, #{content}, #{likeCount}, #{status}, NOW())
    </insert>

    <update id="update">
        UPDATE post_comment
        <set>
            <if test="content != null">content = #{content},</if>
            <if test="likeCount != null">like_count = #{likeCount},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM post_comment WHERE id = #{id}
    </delete>
</mapper>
